# FlatNotes 环境变量配置示例
# 复制此文件为 .env 并修改相应的值

# 认证密码 - 用于保护笔记应用的访问
# 生产环境必须使用强密码（至少12位，包含大小写字母、数字和特殊字符）
# 示例：MySecureP@ssw0rd2024!
AUTH_PASSWORD=your_very_strong_password_here

# JWT 密钥 - 用于生成和验证 JWT token
# 必须设置！建议使用至少64位的随机字符串
# 可以使用以下命令生成: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# 示例：a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456789012345678901234567890abcdef
JWT_SECRET=your_256_bit_secret_key_here

# 服务器端口（可选，默认为 3001）
PORT=3001

# 运行环境（development/production）
NODE_ENV=production

# CORS 配置 - 仅用于本地开发环境
# 设置为 true 可允许所有来源的请求，解决本地开发时的 CORS 问题
# 生产环境请勿设置此项或设置为 false
# CORS_ALLOW_ALL=true